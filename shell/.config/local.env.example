# Machine-local secrets, sourced by .profile if present.
# Copy to ~/.config/local.env (gitignored) and fill in real values.

# export VLLM_API_KEY="..."

# Used by install.sh to configure Claude MCP servers (claude mcp add).
# export BRAVE_API_KEY="..."
# export SEMANTIC_SCHOLAR_API_KEY="..."
